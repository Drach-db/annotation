# Backend для аннотации видео с Qwen3-VL

Простой Python скрипт для аннотации видео с использованием модели Qwen3-VL через DashScope API.

## 🚀 Быстрый старт

### 1. Установите зависимости:
```bash
pip install dashscope opencv-python numpy
```

### 2. Настройте параметры в начале файла annotate_video.py:
```python
# ОСНОВНЫЕ НАСТРОЙКИ - ИЗМЕНИТЕ ЗДЕСЬ
API_KEY = 'ваш-ключ-здесь'  # Ваш API ключ
MODEL = 'qwen-vl-max-latest'  # или 'qwen-vl-plus-latest'
FPS = 1.0  # Частота извлечения кадров
TEMPERATURE = 0.3  # Для генерации текста
MAX_TOKENS = 4000  # Максимум токенов
```

### 3. Запустите аннотацию:
```bash
python annotate_video.py videos/ваше_видео.mp4
```

## 📝 Использование

### Базовый запуск:
```bash
python annotate_video.py videos/example.mp4
```

### С кастомным промптом:
```bash
python annotate_video.py videos/example.mp4 --prompt prompts/custom.txt
```

### С изменением параметров через командную строку:
```bash
python annotate_video.py videos/example.mp4 --fps 2.0 --temperature 0.5
```

## 📁 Структура проекта

```
backend/
├── annotate_video.py         # Основной скрипт (настройки в начале файла)
├── requirements.txt          # Python зависимости
├── videos/                   # Поместите видео сюда
├── prompts/                  # Промпты для аннотации
│   └── pov_annotation_prompt.txt
└── outputs/                  # Результаты (JSON и TXT)
```

## ⚙️ Параметры

Все основные настройки находятся **в начале файла annotate_video.py**:

- `API_KEY` - Ваш DashScope API ключ
- `MODEL` - Модель ('qwen-vl-max-latest' или 'qwen-vl-plus-latest')
- `FPS` - Частота кадров для анализа (0.1 - 10.0)
- `TEMPERATURE` - Параметр генерации (0.0 - 1.0)
- `MAX_TOKENS` - Максимум токенов в ответе

## 📊 Формат результатов

Результаты сохраняются в папке `outputs/` в двух форматах:

### JSON (для программной обработки):
```json
{
  "video": "example.mp4",
  "model": "qwen-vl-max-latest",
  "fps": 1.0,
  "processing_time": 12.5,
  "timestamp": "20241112_180000",
  "annotation": "Детальное описание..."
}
```

### TXT (для чтения):
```
=== Аннотация видео: example.mp4 ===
Модель: qwen-vl-max-latest
FPS: 1.0
Время обработки: 12.5 сек
Дата: 20241112_180000

--- Аннотация ---
Детальное описание действий...
```

## 📌 Важно

- **Максимальный размер видео**: 1 GB (ограничение DashScope)
- **Поддерживаемые форматы**: MP4, MOV, AVI, WebM
- **API ключ**: Получите на [DashScope Console](https://dashscope.console.aliyun.com/)

## 🔧 Решение проблем

### "API Error (401): Invalid API key"
Проверьте API ключ в начале файла annotate_video.py

### "Video file not found"
Убедитесь, что видео находится в указанном пути

### Долгая обработка
Уменьшите FPS или используйте модель qwen-vl-plus-latest

## 📄 Лицензия

MIT